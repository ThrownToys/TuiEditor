rules = {
    "plaintextOr"
    "plaintextAnd"
}

searchExpression = {
    plaintextOr = " || "
    plaintextAnd = " && "
}

errorMessage = {
    plaintextOr = "Use `or` instead of `||`"
    plaintextAnd = "Use `and` instead of `&&`"
}
